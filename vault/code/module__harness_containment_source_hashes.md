---
kind: module
id: "module:harness.containment.source_hashes"
title: "The register C15 clause 3 compares against, and the reason it had nothing to compare."
shape: "module"
present: "true"
protected: "true"
lint_gated: "false"
source: "harness/containment/source_hashes.py:1"
extractor: "code"
tags: [protected]
aliases:
  - "The register C15 clause 3 compares against, and the reason it had nothing to compare."
  - "harness.containment.source_hashes"
generated: true
---

# The register C15 clause 3 compares against, and the reason it had nothing to compare.

> [!warning] Generated — do not edit
> This note is emitted by `tools/gen_vault.py` from the repository. Edit the source, then regenerate. `gen_vault.py --check` fails on a hand edit.

**Source** · `harness/containment/source_hashes.py:1`

## Fields

| Field | Value |
|---|---|
| `is_test` | false |
| `path` | harness/containment/source_hashes.py |
| `tree` | harness |

## Binds

- [[module__harness_containment|harness.containment]] **contains** → this
- [[module__harness_containment_patch_side|C15 — the oracle arriving through the deliverable channel.]] **imports** → this
- [[module__harness_containment_test_c_assertions|C1–C15 beyond the two probes, each paired with the control that stops it reading green.]] **imports** → this

## Enforced by (code)

- [[decision__D54|D50 is enforced by an environment split, not by a check alone: the oracle's outputs cross ]] **enforced_by** → this — """The register C15 clause 3 compares against, and the reason it had nothing to compare.

Clause 3 catches the vendoring
- [[decision__D57|The harness self-test suites are two-sided, and each carries a stated vacuity control]] **enforced_by** → this — # D57 at the loader rather than at the caller. A register that parsed to nothing is a
