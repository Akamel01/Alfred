---
kind: module
id: "module:migrations.harness.control.versions.0002_fingerprint_run_fields"
title: "control: the run-fingerprint fields the register had no column for."
shape: "file"
present: "true"
protected: "false"
lint_gated: "false"
source: "migrations/harness/control/versions/0002_fingerprint_run_fields.py:1"
extractor: "code"
aliases:
  - "control: the run-fingerprint fields the register had no column for."
  - "migrations.harness.control.versions.0002_fingerprint_run_fields"
generated: true
---

# control: the run-fingerprint fields the register had no column for.

> [!warning] Generated — do not edit
> This note is emitted by `tools/gen_vault.py` from the repository. Edit the source, then regenerate. `gen_vault.py --check` fails on a hand edit.

**Source** · `migrations/harness/control/versions/0002_fingerprint_run_fields.py:1`

## Fields

| Field | Value |
|---|---|
| `is_test` | false |
| `path` | migrations/harness/control/versions/0002_fingerprint_run_fields.py |
| `tree` | migrations |

## Enforced by (code)

- [[adr__ADR-0020|The run fingerprint record, and the two assertions that were waiting on it]] **enforced_by** → this — """control: the run-fingerprint fields the register had no column for.

Revision ID: 0002_fingerprint_run_fields
Revises
- [[decision__D19|Autonomy grants are keyed to a fingerprint]] **enforced_by** → this — """control: the run-fingerprint fields the register had no column for.

Revision ID: 0002_fingerprint_run_fields
Revises
- [[decision__D40|fingerprint extension (final form)]] **enforced_by** → this — """control: the run-fingerprint fields the register had no column for.

Revision ID: 0002_fingerprint_run_fields
Revises
