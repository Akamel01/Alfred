---
kind: module
id: "module:bench.toy_tasks"
title: "Phase -1 toy tasks: is the narrow task class within reach of a local model?"
shape: "file"
present: "true"
protected: "false"
lint_gated: "false"
source: "bench/toy_tasks.py:1"
extractor: "code"
aliases:
  - "Phase -1 toy tasks: is the narrow task class within reach of a local model?"
  - "bench.toy_tasks"
generated: true
---

# Phase -1 toy tasks: is the narrow task class within reach of a local model?

> [!warning] Generated — do not edit
> This note is emitted by `tools/gen_vault.py` from the repository. Edit the source, then regenerate. `gen_vault.py --check` fails on a hand edit.

**Source** · `bench/toy_tasks.py:1`

## Fields

| Field | Value |
|---|---|
| `is_test` | false |
| `path` | bench/toy_tasks.py |
| `tree` | bench |

## Enforced by (code)

- [[decision__D10|Hard blast-radius separation]] **enforced_by** → this — """Phase -1 toy tasks: is the narrow task class within reach of a local model?

Each task is shaped like the real Phase 
- [[decision__D36|Acceptance requires held-out pass; retries select only against visible criteria; the agent]] **enforced_by** → this — """Phase -1 toy tasks: is the narrow task class within reach of a local model?

Each task is shaped like the real Phase 
- [[decision__D5|The harness executes checks, never the agent]] **enforced_by** → this — """Phase -1 toy tasks: is the narrow task class within reach of a local model?

Each task is shaped like the real Phase 
