---
kind: risk
id: "risk:R12"
title: "The data licence is unaffordable, so the wedge must not require one"
status: "open"
shape: "heading"
number: "R12"
source: "docs/tier0/risk-register.md:186"
extractor: "charter"
aliases:
  - "R12"
  - "The data licence is unaffordable, so the wedge must not require one"
generated: true
---

# The data licence is unaffordable, so the wedge must not require one

> [!warning] Generated — do not edit
> This note is emitted by `tools/gen_vault.py` from the repository. Edit the source, then regenerate. `gen_vault.py --check` fails on a hand edit.

**Source** · `docs/tier0/risk-register.md:186`

## Fields

| Field | Value |
|---|---|
| `heading` | The data licence is unaffordable, so the wedge must not require one |
