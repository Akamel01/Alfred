"""The criterion plane: materialize from trusted provenance, execute, produce a verdict.

Inspector machinery (D20). Runs **outside the agent's tree** (A1) and imports nothing
from `src/`. The agent produces a claim; this package produces the fact.
"""
