# plan/

This directory mirrors the architecture plan of record: **history, not instruction** — where
it disagrees with `docs/`, the register wins. The mirror file is content-sealed (`manifest.json`
pins its sha256; CI verifies the hash on every runner), so do not edit it; re-sync via the
served page's refresh. It is excluded from factory dispatch workspaces and seeds (the W3
ruling). This README is not sealed — the manifest pins only the mirror file.
