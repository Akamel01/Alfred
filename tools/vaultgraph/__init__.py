"""Alfred's vault and knowledge graph generator. One extraction, several renderers.

The vault this builds is *derived*. Nothing is authored in it that is not authored somewhere
else first, which is the property that makes it admissible as a read model under D44/D47/D51
rather than an unfingerprinted write path into agent context. `--check` is what keeps that
property true rather than merely intended.
"""

from __future__ import annotations

__all__ = ["__version__"]

__version__ = "1"
