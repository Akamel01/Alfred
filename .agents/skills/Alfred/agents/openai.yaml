interface:
  display_name: "Alfred"
  short_description: "Repo-specific patterns and workflows for Alfred"
  default_prompt: "Use the Alfred repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true